import { GET_LINEDATA,ADD_LINEDATA } from '../ActionTypes';

const initialState = {
    lineData : [],
    linedata : 1
}

export const LineData = (state = initialState,action) => {
    switch(action.type){
        default : 
            return state;
    }
}